<?xml version="1.0" encoding="utf-8"?>
<!-- FolioReader loads chapters via HttpURLConnection + WebView to http://127.0.0.1 (R2 streamer).
     A library-only domain-config can fail to merge or apply to all connections on newer SDKs; allow cleartext for localhost explicitly. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
